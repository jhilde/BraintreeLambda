{
  "name": "braintree-lambda",
  "version": "1.0.0",
  "description": "This is a lambda function to create transactions.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "braintree": "^1.37.0",
    "grunt-aws-lambda": "^0.12.0",
    "sendgrid": "^2.0.0"
  },
  "devDependencies": {
      "grunt": "0.4.*",
      "grunt-pack": "0.1.*",
      "grunt-aws-lambda": "0.1.*"
  }
}
